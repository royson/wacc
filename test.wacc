# a simple recursive function

# Output:
# #empty#

# Program:

begin
  int rec(int x) is
    if x == 0 
    then
      skip
    else
      print x ;
      x = 3 ;
      int y = call rec(x - 1)
    fi ;
    return 42  
  end

  int x = 0 ;
  x = call rec(8) ;
  print x
end
